#!/bin/sh
cd backend || exit 0
echo "[pre-push] npm audit (high)..."
npm audit --audit-level=high || { echo "PUSH BLOCKED: high/critical dependency advisory."; exit 1; }
echo "[pre-push] tests..."
npm run test:lumi || { echo "PUSH BLOCKED: tests failing."; exit 1; }
exit 0
