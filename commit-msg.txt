feat(core): major stability fixes, dashboard redesign, and AI voice upgrade

WHAT WAS BROKEN AND FIXED:

BUG FIX - Backend PostgreSQL query access (critical)
- pg library returns {rows:[]} object, not a direct array
- Every route called .map(), .length, [0] directly on result object
- Fixed across 8 route files, 63 locations total
- schedule.js(9), books.js(8), projects.js(11), content.js(10)
- contacts.js(11), goals.js(7), jobs.js(7)
- /api/schedule/today was returning 500 on every request
- All dashboard widgets and streak calculation now work correctly

BUG FIX - Infinite 401 authentication loop (critical)
- App was retrying failed auth requests hundreds of times per second
- Added _retry flag so no request retries more than once
- Added isRefreshing flag to prevent simultaneous token refreshes
- Added failedQueue to handle concurrent requests during refresh
- On refresh failure: localStorage clears, user redirects to /login

BUG FIX - Syntax error in TalkToLumi.jsx line 694
- Mismatched quote in fontFamily CSS took down entire voice screen
- One wrong character crashed the whole component

AI SYSTEM - UPGRADED AND WORKING:
- Removed Gemini API, migrated to new AI provider
- Replaced all deprecated model names across entire codebase
- Centralized AI config to prevent future model name issues
- Lumi voice interface now responds correctly to natural language
- Voice-to-text transcription pipeline working end to end
- Lumi reads user schedule context before every response

DASHBOARD - FULL VISUAL REDESIGN:
- Replaced old static dashboard with vision-board layout
- Dark theme: deep navy with amber, teal, and purple accents
- 4 stat cards: journal streak, workouts, savings goal, habits
- April activity calendar with completed days marked in amber
- Today schedule with checkable items and strike-through on done
- Budget donut ring with category breakdown bars
- Habit tracker: 7-day dot grid per habit
- Goals section: colour-coded progress bars
- Reading card: current book, page count, daily goal
- Seasonal background switcher: Harmattan, Rainy, Night, Dawn
- Lumi card: breathing orb animation, one-tap action button
- All components use inline styles, zero CSS conflicts

INFRASTRUCTURE:
- All 8 backend migrations confirmed applied on startup
- Auth flow: login, refresh, logout all returning 200
- Journal entries loading correctly
- PostgreSQL connection stable with reconnect on drop

KNOWN REMAINING WORK:
- Journal page UI needs redesign (planned next sprint)
- Dashboard still uses static mock data, real API connections next
- Social media post scheduler and AI automation not yet started
- Screenshots to be added to /docs/screenshots/